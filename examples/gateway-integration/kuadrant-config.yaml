apiVersion: v1
kind: ConfigMap
metadata:
  name: kuadrant-gateway-config
  namespace: default
data:
  gateway-name: my-gateway
  gateway-namespace: gateway-system
  hostname: mcp.example.com
  tool-prefix: myserver_